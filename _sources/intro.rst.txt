.. _intro:

============
Introduction
============

**django-general-ledger** is a Django application which provides a general ledger for accounting purposes. It is designed to be used in conjunction with a Django project which requires accounting functionality. The application is designed to be flexible and can be used in a variety of ways. It is designed to be easy to use and easy to extend.

The application is designed to be used in conjunction with a Django project which requires accounting functionality. The application is designed to be flexible and can be used in a variety of ways. It is designed to be easy to use and easy to extend.

.. py:function:: enumerate(sequence[, start=0])

   Return an iterator that yields tuples of an index and an item of the
   *sequence*. (And so on.)