{{ entry.trans_date.strftime('%b  ').ljust(5) }}{{ entry.trans_date.strftime('%-d').rjust(2) }}  {{ entry.get_counter_entry().ljust(26) }}|{% if entry.is_debit %}{{ (entry.amount|string).rjust(num_width) }}{% else %}{{ " " * num_width }}{% endif %}|{% if entry.is_credit %}{{ (entry.amount|string).rjust(num_width)  }}{% else %}{{ " " * num_width }}{% endif %}|{{ (entry.running_balance()|string).rjust(num_width) }}
